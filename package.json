{
  "name": "csv-challenge",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc --noEmit && esbuild index.ts --bundle --platform=node --target=node16 --keep-names --minify --sourcemap --target=es2022 --outfile=cli.js",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "esbuild": "^0.19.2",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "fast-csv": "^4.3.6"
  }
}
